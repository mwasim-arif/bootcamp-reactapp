import React from 'react';


function Alpha (props) {
    return(
        <div>
            <h1>In the history {props.empireName} has significan area.</h1>
            <h1>When {props.empireName} started to make wrong decisions and not able to maintain justice, they lost hold.</h1>

        </div>
    )
}

export default Alpha;